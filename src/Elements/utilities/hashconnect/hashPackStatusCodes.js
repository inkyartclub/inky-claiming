const hashPackStatusCodes = {
  USER_REJECT: "The transaction was rejected by the user",
  NOT_SUPPORTED:
    "That type of transaction is not supported by HashPack currently",
};

export default hashPackStatusCodes;
